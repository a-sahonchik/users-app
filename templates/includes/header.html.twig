<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="{{ path('index') }}">Home</a>
  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="nav">
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="signed-user-info">
          <span class="user-login">Hello, {{ app.user.email }} </span>
          <a class="nav-link logout-button" href="{{ path('app_logout') }}">logout</a>
        </div>
      {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_login') }}">Sign in</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_register') }}">Sign up</a>
        </li>
      {% endif %}
    </ul>
  </div>
</nav>
